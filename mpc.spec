# 
# Do NOT Edit the Auto-generated Part!
# Generated by: spectacle version 0.24.1
# 

Name:       mpc

# >> macros
# << macros

Summary:    A multiprecision library
Version:    0.9
Release:    1
Group:      Development/Libraries
License:    LGPL2.1
URL:        http://www.multiprecision.org/
Source0:    http://www.multiprecision.org/mpc/download/%{name}-%{version}.tar.gz
Source100:  mpc.yaml
Patch0:     mpc-r1119-rebased-build-fix.patch
Requires(post): /sbin/install-info
Requires(post): /sbin/ldconfig
Requires(postun): /sbin/install-info
Requires(postun): /sbin/ldconfig
BuildRequires:  gmp-devel >= 4.3.2
BuildRequires:  mpfr-devel >= 2.4.2

%description
Mpc is a C library for the arithmetic of complex numbers with arbitrarily 
high precision and correct rounding of the result. It is built upon and 
follows the same principles as Mpfr. The library is written by Andreas 
Enge, Philippe Theveny and Paul Zimmermann and is distributed under r the
Gnu Lesser General Public License, either version 2.1 of the licence, or 
(at your option) any later version.


%package devel
Summary:    Development tools for the MPC Library
Group:      Development/Libraries
Requires:   %{name} = %{version}-%{release}
Requires:   %{name} = %{version}-%{release}, gmp-devel, mpfr-devel

%description devel
The header files, documentation and static libraries for developing
applications using the MPC Library.



%prep
%setup -q -n %{name}-%{version}

# mpc-r1119-rebased-build-fix.patch
%patch0 -p1
# >> setup
# << setup

%build
# >> build pre
autoreconf --install --force
# << build pre

%reconfigure --disable-static

# >> build post
make
# << build post

%install
rm -rf %{buildroot}
# >> install pre
# << install pre

# >> install post
%make_install
# << install post


%post
/sbin/ldconfig
%install_info --info-dir=%_infodir %{_infodir}/mpc.info.gz

%postun
/sbin/ldconfig
if [ $1 = 0 ] ;then
%install_info_delete --info-dir=%{_infodir} %{_infodir}/mpc.info.gz
fi

%files
%defattr(-,root,root,-)
# >> files
%{_libdir}/libmpc.so.*
%{_infodir}/mpc.info.gz
# << files

%files devel
%defattr(-,root,root,-)
# >> files devel
%{_libdir}/libmpc.so
%{_includedir}/mpc.h
# << files devel
